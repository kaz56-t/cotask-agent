実装TODOリスト（5フェーズ）
Phase 1: 基盤構築と疎通確認
タスク: Docker Compose設定、FastAPI（バックエンド）とNext.js（フロントエンド）の最小構成作成。

ゴール: 画面の入力欄から「こんにちは」と送ると、サーバーから「[定型文] 受信しました」と返ってくること。

人間のテスト事項:

[x] docker-compose up だけで、フロントとバックが両方正常に起動するか？

[x] フロントからAPIを叩いた際、CORSエラーが出ずにレスポンスが返るか？

Phase 2: LangGraphによる要件定義チャット & DB保存
タスク: LangGraphを用いた対話フローの実装。SQLite/SQLAlchemyによるチャット履歴の永続化。

ゴール: ユーザーの曖昧な指示に対し、AIが質問を返し、そのやり取りがリロード後も保持されていること。

人間のテスト事項:

[x] DB（SQLite）にチャット履歴が保存され、ブラウザをリロードしても過去のログが表示されるか？

[x] LangGraphの状態管理により、文脈に応じた対話ができているか？

Phase 3: マルチタスクUIの実装
タスク: フロントエンドのサイドバー実装。複数タスクの並列表示と状態（定義中/実行中）の管理。

ゴール: 左側の「＋」ボタンで新しいタスクを追加でき、それぞれのタスクが独立したチャット画面を持つこと。

人間のテスト事項:

[x] サイドバーでタスクを切り替えた際、右側のチャット内容が正しく切り替わるか？

[x] 新規タスクを作成したとき、既存のタスクに影響を与えず独立して開始できるか？

Phase 4: 文書生成・検索エージェントの実装（同一コンテナ内）
タスク: AutoGenまたはLangGraphによる実行エージェントの実装。OpenAI/ClaudeのAPI連携。

ゴール: 「定義」が終わったタスクに対し、AIが実際に文章作成や（モックの）検索を行い、結果を出力すること。

人間のテスト事項:

[x] .env に設定したAPIキーでOpenAI/Claudeが正しく動作するか？

[x] UIからモデルを選択し、挙動が変わることを確認できるか？

Phase 5: 成果物のダウンロード
タスク: 要件が決まったタスクについて、「開始」ボタンを押した際に、タスクが開始され、成果物がダウンロードできること。

ゴール: タスクが完了した際に、成果物がダウンロードできること。

人間のテスト事項:

[x] 要件が決まったタスクについて、「開始」ボタンを押した際に、タスクが開始され、成果物がダウンロードできること。


Phase 6: 実行環境のコンテナ分離 (Docker-in-Docker)
タスク: エージェントのコード実行部分を別コンテナ（Runtime）へ切り出し。成果物ファイルの受け渡し実装。

ゴール: AIが生成したPythonコードが専用コンテナで走り、生成されたファイルがホスト側に保存・ダウンロードできること。

人間のテスト事項:

[ ] 実行コンテナ内で pip install 等を行っても、メインのバックエンドコンテナが汚染されないか？

[ ] 生成されたCSVや画像ファイルが、フロントエンドのリンクから正常にダウンロードできるか？